{
  "name": "signalpipe",
  "private": true,
  "workspaces": ["packages/*", "services/*"],
  "scripts": {
    "build": "tsc -b",
    "test": "vitest run",
    "up": "docker compose up --build",
    "down": "docker compose down -v",
    "probe": "node ops/probe.mjs"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.7.0",
    "vitest": "^2.1.0"
  }
}
